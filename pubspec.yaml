name: flutter_login_page
description: A simple login page created with Flutter.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  mockito: ^5.0.16
  provider: ^6.0.1

dev_dependencies:
  build_runner: ^2.1.5
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
